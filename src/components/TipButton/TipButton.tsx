import React from 'react';
import { IProps } from './types';
import styles from './TipButton.module.css';

const TipButton = (props: IProps) => {
  return <div className={styles.wrapper}>TODO</div>;
};

export default TipButton;
